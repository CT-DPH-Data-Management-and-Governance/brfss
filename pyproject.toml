[project]
name = "brfss"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bs4>=0.0.2",
    "pandas>=2.3.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
